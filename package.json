{
  "name": "typescript-workspace-draft",
  "version": "1.0.0",
  "description": "typescript-workspace-draft ==",
  "main": "index.js",
  "scripts": {
    "// build": "tsc --build --verbose",
    "build:all": "npm run build -ws --if-present",
    "clean:all": "npm run clean -ws --if-present",
    "pretest:all": "npm run build:all",
    "test:all": "npm test -ws --if-present"
  },
  "keywords": [],
  "author": "Mitch Allen",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "packages/core",
    "packages/alpha",
    "packages/beta"
  ]
}
